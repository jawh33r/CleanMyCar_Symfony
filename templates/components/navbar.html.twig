<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_home') }}">
            <i class="bi bi-car-front"></i> CleanMyCar
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home') }}">
                        <i class="bi bi-house"></i> Home
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_services') }}">
                        <i class="bi bi-tools"></i> Services
                    </a>
                </li>
                <li class="nav-item">
                    {% if app.user %}
                        <a class="nav-link" href="{{ path('app_reservation_new') }}">
                            <i class="bi bi-calendar-plus"></i> Réserver
                        </a>
                    {% else %}
                        <a class="nav-link" href="{{ path('app_login') }}">
                            <i class="bi bi-calendar-plus"></i> Réserver
                        </a>
                    {% endif %}
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_contact') }}">
                        <i class="bi bi-envelope"></i> Contact
                    </a>
                </li>
            </ul>
            <ul class="navbar-nav">
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" data-bs-auto-close="true" aria-expanded="false">
                            <i class="bi bi-person-circle"></i> {{ app.user.email }}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                            {% if 'ROLE_ADMIN' in app.user.roles %}
                                <li><a class="dropdown-item" href="{{ path('app_admin_dashboard') }}">
                                    <i class="bi bi-speedometer2"></i> Dashboard
                                </a></li>
                                <li><a class="dropdown-item" href="{{ path('app_admin_index') }}">
                                    <i class="bi bi-person-badge"></i> Admin Panel
                                </a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="{{ path('app_logout') }}">
                                    <i class="bi bi-box-arrow-right"></i> Déconnexion
                                </a></li>
                            {% elseif 'ROLE_OVR' in app.user.roles %}
                                <li><a class="dropdown-item" href="{{ path('app_ouvrier_profile_index') }}">
                                    <i class="bi bi-person"></i> Mon Profil
                                </a></li>
                                <li><a class="dropdown-item" href="{{ path('app_ouvrier_reservations_index') }}">
                                    <i class="bi bi-calendar-check"></i> Mes Réservations
                                </a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="{{ path('app_logout') }}">
                                    <i class="bi bi-box-arrow-right"></i> Déconnexion
                                </a></li>
                            {% else %}
                                <li><a class="dropdown-item" href="{{ path('app_profile_index') }}">
                                    <i class="bi bi-person"></i> Mon Profil
                                </a></li>
                                <li><a class="dropdown-item" href="{{ path('app_reservation_index') }}">
                                    <i class="bi bi-calendar-check"></i> Mes Réservations
                                </a></li>
                                <li><a class="dropdown-item" href="{{ path('app_reservation_new') }}">
                                    <i class="bi bi-calendar-plus"></i> Nouvelle Réservation
                                </a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="{{ path('app_logout') }}">
                                    <i class="bi bi-box-arrow-right"></i> Déconnexion
                                </a></li>
                            {% endif %}
                        </ul>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">
                            <i class="bi bi-box-arrow-in-right"></i> Connexion
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">
                            <i class="bi bi-person-plus"></i> Inscription
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_login') }}">
                            <i class="bi bi-shield-lock"></i> Admin
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
